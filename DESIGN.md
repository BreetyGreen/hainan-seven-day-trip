# Design System

## Physical Scene

九月海边，旅行者在车旁用手机确认下一站。阳光强、网络可能波动，因此地图、路线筛选与活动弹窗必须一眼可读。

## Direction

参考 Polarsteps 的地图旅程结构，但使用自己的视觉语言：底图占据舞台，左侧只保留路线与两基地摘要，具体玩法由地图节点原地弹出。采用深海蓝、珊瑚红和浅海青，不做旅游杂志宫格，也不保留长篇右侧详情面板。

## Color Strategy

```css
:root {
  --navy: #071b26;
  --paper: #f5f2eb;
  --white: #fffefa;
  --ink: #10232b;
  --coral: #f45f52;
  --cyan: #64d0ca;
}
```

深海蓝承担交通与住宿基地，珊瑚红标记当前自驾路线和主要动作，浅海青表示提示与辅助信息。

## Typography

全部使用系统中文无衬线字体；标题不小于 20px，弹窗正文不小于 10px，主要摘要正文 12px。标题字距不低于 `-0.04em`，长文本控制在 65–75 字符宽度内。

## Layout

- 桌面端使用全屏地图，左侧 360px 图文摘要卡，节点弹窗最大宽度约 340px。
- 顶栏为全程、D1–D7 分段控件；初始状态固定为全程。
- 手机端地图继续占满剩余视口，摘要卡压缩到底部；弹窗打开时摘要自动退场。
- 单人/双人切换只影响驾驶与安全建议，不引入费用账本。

## Motion

首次进入全程地图后保持静止，由地图上的旅程封面承接开始动作；点击“开始七日旅程”后，前景路线在独立 Canvas 上逐段绘制，旅行者标记沿真实 GeoJSON 路径移动，镜头在日切换、行进安全区和抵达节点之间跟随。抵达景点、海岸、餐食、文化、观景节点或第一次进入住宿基地时，播放自动暂停并显示完整活动卡；卡片底部固定“继续前往下一站”，点击后才恢复。机场、重复回酒店等过渡节点短暂停留但不弹窗。顶部 D1–D7 时间轴显示当前与已完成节点，底部提供播放、暂停、继续和重播。用户拖拽地图会暂停，按天筛选会退出全程播放。

Leaflet 缩放继续使用官方 250ms transform 过渡，滚轮输入做 60ms 防抖，缩放中不重建瓦片；静态路线和播放路线分别使用 Canvas，播放段最多 140 个坐标。镜头只在路段开始时切换空间尺度，行进中只平移：航班 zoom 6、跨城 zoom 9、35 km 内近郊 zoom 11、同城 zoom 13；抵达机场 zoom 9、酒店 zoom 13、活动地点 zoom 15。地图节点悬停在 200ms 内上浮并展开状态光环；点击地点卡以 220ms 淡入。`prefers-reduced-motion` 下播放改为快速按节点跳转，仍保留手动开始和关键到站确认。

## Components

- `ModeSwitch`：一人/二人驾驶提示。
- `RouteFilter`：完整路线与 D1–D7 筛选。
- `RouteMap`：OpenStreetMap、9 个单一交通方式的本地 GeoJSON 路段、路线动画、飞机/汽车/港口图标和实景标记。
- `JourneyStartGate`：地图上的旅程封面和唯一初次播放入口，避免页面加载即自动移动。
- `ActivityPopup`：360px 宽的地图内浮层；有对应实景时先显示大图，再显示时间、步骤、停车与九月退路。打开时只给地图增加 6% 深海蓝蒙层，左侧计划保持 100% 清晰与可操作，不再用透明度模拟禁用。
- `PlaceDetailDialog`：站内完整详情；顶部固定当天第几站与“上一站 → 当前 → 下一站”，并列出今日剩余节点。活动节点使用本地实景图，外链只保留为原始资料证据。
- `HotelStory`：紧凑的横向酒店图文条；默认展示图片、住宿定位和来源，选择理由与入住提醒按需展开。
- `BaseFlow`：万宁三晚、三亚三晚以及处于两张酒店图鉴之间的 Day 4 唯一换宿连接器。
- `TravelBasics`：吃、穿与驾驶提示。

## Responsive and States

360px 起可用。手机端酒店图鉴横向滑动，Day 4 换宿卡保持双图可见；地点浮层宽度不超过视口减 36px。交通方式与换宿徽章不接管鼠标事件，确保任何重叠位置的地点节点仍可点击。地图瓦片失败时显示本地路线状态；有对应照片的节点显示“实景”标记并在弹窗展示本地小红书图片，其他节点只用真实文字与坐标，不用装饰性假图。
